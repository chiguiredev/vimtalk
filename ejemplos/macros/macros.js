function foo (arg1, arg2, arg3) {
  cons bar = {
    greeting: 'Hello flisol",
    name: 'Rafael Salas',
    age: 22
  }
}

// Camnbiemos  esta funcio a arrow function con un macro

function foo (arg1, arg2, arg3) {
  cons bar = {
    name: 'Rafael',
    lastName: 'Salas',
    age: 22
  }
  return bar;
}

function sayHello (nombre) {
  return `Hello ${nombre}, welcome to the vim talk`;
}

function add (a, b) {
  return a + b;
}
